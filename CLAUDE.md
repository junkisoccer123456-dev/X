# X投稿生成システム

- `/x-account <名前>` でアカウント情報を `accounts/<名前>.md` に登録し、`/x-post <名前>` で構文を指定して投稿を生成する。手順は `.claude/skills/` の SKILL.md に従う。
- 構文の定義は `structures/<構文名>.md`。新しい構文を受け取ったらここに保存する。
- 投稿の経歴・実績・数値は `accounts/` にある事実だけを使い、創作しない。
- `accounts/` と `structures/` を変更したら、コミットしてプッシュする（クラウド環境は終了すると消えるため）。
